/* eslint-disable no-underscore-dangle */
/* eslint-disable no-undef */
// import proxyquire from 'proxyquire';

describe('install-cmd', () => {
  // TODO: Write unit tests for unexposed functions
  // const install = proxyquire('../../src/commands/install-cmd.ts', {});
  it('returns correct helm install command', async () => {
    // const result = install.buildHelmInstallCmd('my-release', 'bitnami/mongodb', {});
    // expect(result).toBe('helm install my-release bitnami/mongodb ');
  });
});
